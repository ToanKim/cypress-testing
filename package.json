{
  "name": "cypress-testing",
  "version": "1.0.0",
  "description": "A Cypress Testing Learning",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "init": "npm install && npm run cypress:open"
  },
  "author": "toan.trieu",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^10.11.0"
  }
}
